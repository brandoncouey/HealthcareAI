"use client"

import * as RechartsPrimitive from "recharts"

const Chart = RechartsPrimitive.ResponsiveContainer

const ChartGrid = RechartsPrimitive.CartesianGrid

const ChartXAxis = RechartsPrimitive.XAxis

const ChartYAxis = RechartsPrimitive.YAxis

const ChartTooltip = RechartsPrimitive.Tooltip

const ChartLegend = RechartsPrimitive.Legend

const ChartLine = RechartsPrimitive.Line

const ChartArea = RechartsPrimitive.Area

const ChartBar = RechartsPrimitive.Bar

const ChartPie = RechartsPrimitive.Pie

const ChartCell = RechartsPrimitive.Cell

const ChartScatter = RechartsPrimitive.Scatter

const ChartComposed = RechartsPrimitive.ComposedChart

const ChartBrush = RechartsPrimitive.Brush

const ChartReferenceLine = RechartsPrimitive.ReferenceLine

const ChartReferenceArea = RechartsPrimitive.ReferenceArea

const ChartFunnel = RechartsPrimitive.Funnel

const ChartRadar = RechartsPrimitive.Radar

const ChartRadialBar = RechartsPrimitive.RadialBar

const ChartZAxis = RechartsPrimitive.ZAxis

const ChartErrorBar = RechartsPrimitive.ErrorBar

const ChartLabel = RechartsPrimitive.Label

const ChartLabelList = RechartsPrimitive.LabelList

const ChartSector = RechartsPrimitive.Sector

const ChartCurve = RechartsPrimitive.Curve

const ChartPolygon = RechartsPrimitive.Polygon

const ChartDot = RechartsPrimitive.Dot

const ChartCross = RechartsPrimitive.Cross

const ChartSymbols = RechartsPrimitive.Symbols

const ChartPolarAngleAxis = RechartsPrimitive.PolarAngleAxis

const ChartPolarGrid = RechartsPrimitive.PolarGrid

const ChartPolarRadiusAxis = RechartsPrimitive.PolarRadiusAxis

const ChartSurface = RechartsPrimitive.Surface

const ChartLayer = RechartsPrimitive.Layer

const ChartRectangle = RechartsPrimitive.Rectangle

export {
  Chart,
  ChartGrid,
  ChartXAxis,
  ChartYAxis,
  ChartTooltip,
  ChartLegend,
  ChartLine,
  ChartArea,
  ChartBar,
  ChartPie,
  ChartCell,
  ChartScatter,
  ChartComposed,
  ChartBrush,
  ChartReferenceLine,
  ChartReferenceArea,
  ChartFunnel,
  ChartRadar,
  ChartRadialBar,
  ChartZAxis,
  ChartErrorBar,
  ChartLabel,
  ChartLabelList,
  ChartSector,
  ChartCurve,
  ChartPolygon,
  ChartDot,
  ChartCross,
  ChartSymbols,
  ChartPolarAngleAxis,
  ChartPolarGrid,
  ChartPolarRadiusAxis,
  ChartSurface,
  ChartLayer,
  ChartRectangle,
}
